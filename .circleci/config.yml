version: 2.1
orbs:
  node: circleci/node@2.1.1
jobs:
  build-and-test:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: latest
      - run: node --version
      - run: yarn install
      - run: yarn test
  deploy-rc:
    docker:
      - image: 'cimg/base:stable'
    branches:
      only:
        - /rc-.*/
    steps:
      - checkout
      - node/install:
          install-yarn: true
          node-version: latest
      - run:
          name: Authenticate with NPM registry
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > .npmrc
      - run:
          name: Publish packages to NPM
          command: yarn deploy --preid next --dist-tag next
workflows:
  build-and-test:
    jobs:
      - build-and-test
      - deploy-rc
