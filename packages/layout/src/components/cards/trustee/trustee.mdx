---
name: Trustee
menu: Cards
route: /cards/trustee
---

import { Props } from 'docz';
import { Playground, experianApiGet } from '@playground';
import { Form } from 'react-final-form';
import { useState } from 'react';
import { TrusteeCard } from './trustee';

# Trustee Card

Trustee Card Card for tpr apps

<!-- ## Properties

<Props of={Trustee} /> -->

## Usage

```js
import { TrusteeCard } from '@tpr/layout';
```

## Basic usage

<Playground>
	{() => {
		const [complete, setComplete] = useState(false);
		const callbackFn = (values, _fullTrusteeObject) => {
			console.log(values);
			console.log(_fullTrusteeObject);
			return Promise.resolve(values);
		};
		const trustee = {
			id: '12345',
			schemeRoleId: '12345',
			//
			title: 'Mr',
			firstname: 'John',
			lastname: 'Smith',
			trusteeType: 'member-nominated',
			isProfessionalTrustee: false,
			//
			addressLine1: 'The Pensions Regulator',
			addressLine2: 'Napier House',
			addressLine3: 'Trafalgar Pl',
			postTown: 'Brighton',
			postCode: 'BN1 4DW',
			county: 'West Sussex',
			country: '',
			countryId: '',
			//
			telephoneNumber: '01273 000 111',
			emailAddress: 'fred.sandoors@trp.gov.uk',
			effectiveDate: '1997-04-01T00:00:00',
		};
		return (
			<TrusteeCard
				onDetailsSave={callbackFn}
				onContactSave={callbackFn}
				onAddressSave={callbackFn}
				onRemove={callbackFn}
				onCorrect={(value) => setComplete(value)}
				addressAPI={{
					get: (endpont) => experianApiGet(endpont),
					limit: 100,
				}}
				complete={complete}
				i18n={{
					preview: {
						buttons: {
							one: 'Trustee',
						},
					},
				}}
				trustee={trustee}
			/>
		);
	}}
</Playground>
