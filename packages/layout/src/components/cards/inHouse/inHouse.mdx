---
name: InHouse
menu: Cards
route: /cards/inHouseAdmin
---

import { Props } from 'docz';
import { Playground, experianApiGet } from '@playground';
import { Form } from 'react-final-form';
import { useState } from 'react';
import { InHouseCard } from './inHouse';

# In House Admin Card

In House Admin Card for tpr apps

## Usage

```js
import { InHouseCard } from '@tpr/layout';
```

## Basic usage

<Playground>
	{() => {
		const [complete, setComplete] = useState(false);
		const callbackFn = (values, _fullInHouseAdminObject) => {
			console.log(values);
			console.log(_fullInHouseAdminObject);
			return Promise.resolve(values);
		};
		const inHouseAdmin = {
			id: '',
			schemeRoleId: 123,
			title: 'Mr',
			firstname: 'John',
			lastname: 'Smoth',
			effectiveDate: '1997-04-01T00:00:00',
			address: {
				addressLine1: 'Napier House',
				addressLine2: 'Trafalgar Pl',
				addressLine3: '',
				postTown: 'Brighton',
				postcode: 'BN1 4DW',
				county: 'West Sussex',
				countryId: ''
			},
			telephoneNumber: '01273 222 111',
			emailAddress: 'john.wick@warnerbros.com',
		};
		return (
			<InHouseCard
				onSaveContacts={callbackFn}
				onSaveAddress={callbackFn}
				onSaveName={callbackFn}
				onRemove={callbackFn}
				onCorrect={(value) => setComplete(value)}
				complete={complete}
				inHouseAdmin={inHouseAdmin}
				addressAPI={{
					get: (endpont) => experianApiGet(endpont),
					limit: 100,
				}}
			/>
		);
	}}
</Playground>
