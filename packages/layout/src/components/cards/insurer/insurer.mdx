---
name: Insurer
menu: Cards
route: /cards/insurer
---

import { Props } from 'docz';
import { Playground } from '@playground';
import { Form } from 'react-final-form';
import { useState } from 'react';
import { InsurerCard } from './insurer';

# Insurer Card

Insurer Card Card for tpr apps

## Usage

```js
import { InsurerCard } from '@tpr/layout';
```

## Basic usage

<Playground>
	{() => {
		const [complete, setComplete] = useState(false);
		const callbackFn = (values, _fullInsurerObject) => {
			console.log(values);
			console.log(_fullInsurerObject);
			return Promise.resolve(values);
		};
		const insurer = {
			id: '',
			schemeRoleId: 123,
			effectiveDate: '1997-04-01T00:00:00',
			organisationReference: 123,
			organisationName: 'Nike Inc.',
			insurerCompanyReference: '12345678',
			telephoneNumber: '01273 222 111',
			emailAddress: 'john.wick@warnerbros.com',
			address: {
				addressLine1: 'Napier House',
				addressLine2: 'Trafalgar Pl',
				addressLine3: '',
				postTown: 'Brighton',
				postcode: 'BN1 4DW',
				county: 'West Sussex',
				countryId: ''
			},
		};
		return (
			<InsurerCard
				onSaveRef={callbackFn}
				onRemove={callbackFn}
				onCorrect={(value) => setComplete(value)}
				complete={complete}
				insurer={insurer}
				preValidatedData={true}
			/>
		);
	}}
</Playground>
